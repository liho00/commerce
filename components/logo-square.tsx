import clsx from 'clsx';
import LogoIcon from './icons/logo';

export default function LogoSquare({ size }: { size?: 'sm' | undefined }) {
  return (
    <>
      <img src="/img/E-Pray_Logo.png" alt="Logo" className='w-16 h-16' />
    </>
  );
}
